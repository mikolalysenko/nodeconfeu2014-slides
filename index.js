'use strict'

//Reveal.js interface
Reveal.addEventListener("slidechanged", function(event) {
  if(event.previousSlide) {
  }
  if(event.currentSlide) {
  }
})
Reveal.initialize({
  width: 1024,
  height: 768,
  slideNumber: true,
  history: true
})